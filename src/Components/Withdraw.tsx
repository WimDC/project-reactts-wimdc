import { Box, Button } from "@mui/material"

export const Withdraw = (setCarStockWim, carStockWim, withdrawOne) => {



    return (
        <Box>
            <Button onClick={() => withdrawOne(item)}></Button>
        </Box>
    )
}